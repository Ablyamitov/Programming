#include<iostream>;
using namespace std;
int main()
{
	setlocale(LC_ALL, "RUS");
	int k = 12, often_num, red_num,x;
	int a[37];
	for (int i = 0; i <= 36;i++)
		a[i] = 0;
	for (int i = 0;;i++)
	{ 
		cout << "Сигизмунд, введи число: ";
		cin >> x;
		if (x >= 0 & x <= 36)
		{
			a[x] = a[x] + 1;
			if 


		}
		if (x < 0)
		{
			break;
		}








}